use vitals;
use uo;
use os;

program LesserPoison(params)
	
	var who := params[1];
	
	while(1)
		SendSysMessage(who, "You're taking poison damage!");
		ApplyRawDamage(who,3);
		sleep(3);
	endwhile
endprogram