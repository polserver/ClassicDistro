use uo;
use os;
use cfgfile;

program explosion_timer(parms)

  var i := 4;
  for( i:=4; i>=1; i-=1 )
    PrintTextAbove( parms[1], CStr( i ) );
    Sleep( 1 );
  endfor
  start_script(":alchemy:exploder", parms);
endprogram